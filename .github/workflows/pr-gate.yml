name: PR gate

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  packages: write
  pull-requests: write

jobs:
  build-image:
    uses: ./.github/workflows/jobs/build-image.yml

  lint:
    needs: build-image
    uses: ./.github/workflows/jobs/lint.yml
    with:
      image: ${{ needs.build-image.outputs.image }}
      pr-number: ${{ github.event.pull_request.number }}

  format:
    needs: build-image
    uses: ./.github/workflows/jobs/format.yml
    with:
      image: ${{ needs.build-image.outputs.image }}
      pr-number: ${{ github.event.pull_request.number }}

  typecheck:
    needs: build-image
    uses: ./.github/workflows/jobs/typecheck.yml
    with:
      image: ${{ needs.build-image.outputs.image }}
      pr-number: ${{ github.event.pull_request.number }}

  vitest:
    needs: build-image
    uses: ./.github/workflows/jobs/vitest.yml
    with:
      image: ${{ needs.build-image.outputs.image }}
      pr-number: ${{ github.event.pull_request.number }}

  build:
    needs: build-image
    uses: ./.github/workflows/jobs/build.yml
    with:
      image: ${{ needs.build-image.outputs.image }}

  playwright:
    needs: build-image
    uses: ./.github/workflows/jobs/playwright.yml
    with:
      image: ${{ needs.build-image.outputs.image }}
      pr-number: ${{ github.event.pull_request.number }}

  # Non-blocking, PR-only — see .github/workflows/jobs/audit.yml.
  audit:
    needs: build-image
    uses: ./.github/workflows/jobs/audit.yml
    with:
      image: ${{ needs.build-image.outputs.image }}
      pr-number: ${{ github.event.pull_request.number }}
